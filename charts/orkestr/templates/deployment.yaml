apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "orkestr.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "orkestr.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "orkestr.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: orkestr
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- if .Values.demo.enabled }}
            - name: ORKESTR_DEMO_MODE
              value: "1"
            - name: ORKESTR_DEMO_WHATSAPP_NUMBER
              value: {{ .Values.demo.whatsappNumber | quote }}
            - name: ORKESTR_WHATSAPP_ACCESS_MODE
              value: "relay"
            {{- with .Values.demo.setupUrl }}
            - name: ORKESTR_DEMO_PUBLIC_SETUP_URL
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.demo.relay.bridgeUrl }}
            - name: ORKESTR_DEMO_WHATSAPP_RELAY_URL
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.demo.relay.accountId }}
            - name: ORKESTR_DEMO_WHATSAPP_RELAY_ACCOUNT_ID
              value: {{ . | quote }}
            {{- end }}
            {{- if .Values.demo.relay.tokenSecretName }}
            - name: ORKESTR_DEMO_WHATSAPP_RELAY_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.demo.relay.tokenSecretName | quote }}
                  key: {{ .Values.demo.relay.tokenSecretKey | quote }}
            {{- end }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "orkestr.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
